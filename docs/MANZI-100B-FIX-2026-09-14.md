## Manzi 100b — letter F schema + PDF demangles

### BEFORE (post-PR #24 source sim / remaining-skips)
| Metric | Count |
|--------|------:|
| Skip remaining (sim) | **244** |
| no-answer | 171 |
| lt2opts | 15 |
| term (excluded OK) | 58 |
| `LAGSTIFNING-4-Q6` | skipped (answer F outside A–E) |
| `data/questions.json` / media-report | still **skippedIncomplete 279** / manziCount **1378** (PR #24 apply not recompiled on main) |

### AFTER (this PR)
| Metric | Count |
|--------|------:|
| New QCM recoveries | **+1** (`LAGSTIFNING-4-Q6` → **F** / SWEDAC) |
| PDF option demangles (cleaner A–F opts) | **4** (LAG-1-Q27 D, LAG-2-Q4 C, LAG-7-Q27 D, S_KERHET-7-Q2009 C) |
| Expected skip remaining (sim) | **~243** (185 incomplete + 58 term) |
| Local slim manzi-only normalize after apply | **manziCount 1413** (Δ≈+35 vs main 1378; includes PR24 source fixes + LAG-4-Q6) |
| `LETTERS` | A–E → **A–F** via apply `ensureLettersF()` |

### Shipped
1. `data/manzi-100b-content-fixes.part0.json` — 5 PDF-verbatim rows
2. `scripts/apply-manzi-100-delta.mjs` — loads `manzi-100b-*.json`; filters incomplete; `ensureLettersF()`
3. `docs/MANZI-UNRECOVERABLE.md` — remaining skips + reasons
4. `docs/MANZI-100B-FIX-2026-09-14.md` — this summary

### PC dig (no deletes)
- **MANZI** `C:\Users\fortu\ManziQA`: MT5 TrendZones QA screenshots — **not taxi exam facit**
- Desktop `scan-banks.ps1` / `scan-banks-out.txt`: script already on box under `/workspace/pc-dump/`; no new BKORT/KARTA questionkeys in existing dumps
- **H7** `Desktop\manzi`: already inventoried; cross-match vs remaining skips → **0 new letter answers**
- This executor has **no Computer/CopyToBox MCP** (Shell stays on Linux box) — live PC re-scan needs parent

### Land (force live bank past 1378)
```bash
npm run manzi:apply
npm run import
```
Do **not** replace `data/questions.json` with the local slim compile alone (researchCount/owner counts differ). Full `import` refreshes research+owner+manzi together.

Constraints: no fake SVGs · no invented answers · Manzi SV verbatim · terms excluded · no PC deletes.
