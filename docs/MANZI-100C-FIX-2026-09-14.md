## Manzi 100c — land compiled bank into `questions.json`

**Coordinates with closed [#23](https://github.com/manzilionellm-dotcom/taxiiii/pull/23)** (Säkerhet book 1 + 18 KARTA recoveries → ~1397). This PR does **not** re-do that work. It lands the already-merged PR #24 / 100b *apply* patches into the **compiled** bank that live still served at **1378**.

Lionel: « fais que ça soit 100 pourcent ».

### BEFORE (main live)
| Metric | Count |
|--------|------:|
| questionsTotal | **1900** |
| manziCount | **1378** |
| skippedIncomplete | **279** |

### AFTER (bank-delta apply)
| Metric | Count |
|--------|------:|
| questionsTotal | **~1940** |
| manziCount | **1413** (Δ **+35**) |
| New invented answers | **0** |

### How
1. `data/manzi-100c-bank-delta.json` — upsert 79 rows (bkort-classic, KARTA facit, LAG-4-Q6/F, S_KERHET renames) + delete 35 stale `SAKERHET-*`
2. `scripts/apply-manzi-100c-bank-delta.mjs` — patches `data/questions.json` + refreshes `media-report.json`
3. `package.json` — runs delta apply at end of `import` / `seed` / `import:coordinator`
4. Optional: `scripts/land-manzi-100c-gz.mjs` + `.gz.b64.parts` if present (MCP size workaround)

### Gold dig (H7 offline → box)
- `htm/kartap/**/questionkey.php.htm` already covered by PR24 / #23
- Remaining Kvibille Q4 + BKORT/korkortA: **no questionkey on disk** — do not invent

### Still-unrecoverable (no-answer)
See `data/manzi-skip-list-100c.json` (~156 no-answer + 58 term + 15 lt2opts).

### Merge note
Safe to merge alongside or after #23. Delta is id-based upsert; #23’s additional recoveries stay if already in `questions.json`.

Constraints: no fake SVGs · no invented answers · Manzi SV verbatim · no PC deletes.
