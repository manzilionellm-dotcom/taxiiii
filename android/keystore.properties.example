# Copy to android/keystore.properties (gitignored) or secrets/keystore.properties.
# Never commit the real file or the .keystore / .jks / .p12.

storeFile=/absolute/path/to/secrets/korkortgo-upload.keystore
storePassword=CHANGE_ME
keyAlias=korkortgo-upload
keyPassword=CHANGE_ME
